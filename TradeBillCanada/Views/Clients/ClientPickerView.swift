import SwiftUI

struct ClientPickerView: View {
    var body: some View {
        Text("Client picker is handled inline in the document editor for the MVP.")
            .foregroundStyle(AppTheme.mutedText)
    }
}

